import TextFieldLayout from './TextFieldLayout';

const Numbers = ({
  t,
  formik,
  handleNextButton,
  question,
  isConfirmButton,
}) => (
  <TextFieldLayout
    t={t}
    tip={question.tipText}
    label={t(question.label)}
    questionCode={question.code}
    handleNextButton={handleNextButton}
    formik={formik}
    isConfirmButton={isConfirmButton}
  >
    {(handleKeyDown) => (
      <input
        id="numbers"
        type="number"
        className={`
                 form-control
                 ${
                   formik.touched[question.code] &&
                   (!formik.errors[question.code] ? 'is-valid' : 'is-invalid')
                 }
               `}
        aria-describedby="numberHelp"
        placeholder={t('common:placeholders.numbers')}
        onKeyDown={handleKeyDown}
        {...formik.getFieldProps(question.code)}
      />
    )}
  </TextFieldLayout>
);

export default Numbers;
