import React from 'react';
import { Story } from '@storybook/react';
import MarketingConsent, { MarketingConsentProps } from './MarketingConsent';

export default {
  title: 'Questions/MarketingConsent',
  component: MarketingConsent,
  argTypes: {
    active: {
      control: false,
    },
    onChange: { action: 'changed' },
    onSubmit: { action: 'submitted' },
  },
};

const Template: Story<MarketingConsentProps> = (args) => (
  <MarketingConsent {...args} />
);

export const Default = Template.bind({});
Default.args = {
  t: (string) => string,
  question: { code: 'tc', optionY: 'Y', optionN: 'N' },
  active: true,
  value: 'Y',
  submitLabel: 'Submit',
  isSubmitting: false,
  eventQuestionDisplay: () => null,
  htmlTop: (
    <div style={{ color: 'blue', textAlign: 'center' }}>
      HTML with text on top
    </div>
  ),
  htmlBottom: (
    <div style={{ color: 'red', textAlign: 'center' }}>
      HTML with text on bottom
    </div>
  ),
};
